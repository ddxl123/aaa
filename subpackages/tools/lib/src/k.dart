import 'dart:ui';

import 'package:flutter/widgets.dart';

final kDialogFixedWidth = MediaQueryData.fromWindow(window).size.width * 0.8;
final kDialogFixedHeight = MediaQueryData.fromWindow(window).size.height * 0.8;
